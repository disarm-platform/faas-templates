provider:
  name: faas
  gateway: http://faas.srv.disarm.io
functions:
  template:
    lang: dockerfile
    handler: ./template
    image: disarm/template:latest
    environment:
      write_timeout: 60
      combine_output: false