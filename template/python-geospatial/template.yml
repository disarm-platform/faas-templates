provider:
  name: faas
  gateway: https://faas.srv.locational.io
functions:
  template:
    lang: dockerfile
    handler: ./template
    image: locational/template:latest
    environment:
      write_timeout: 60
      combine_output: false
template:
  version: 1.0.0